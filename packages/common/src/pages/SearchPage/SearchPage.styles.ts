import { StyleSheet } from 'react-native';

export const SearchPageStyleSheet = StyleSheet.create({
  container: {
    height: '100%',
    display: 'flex',
    backgroundColor: 'gray',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
