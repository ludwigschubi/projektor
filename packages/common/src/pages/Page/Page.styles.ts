import { StyleSheet } from "react-native";

export const PageStyleSheet = StyleSheet.create({
  container: {
    flex: 1,
    height: "100%",
    display: "flex",
    backgroundColor: "white",
    justifyContent: "space-between",
    alignItems: "center",
  },
});
