import { StyleSheet } from "react-native";

export const IconStyleSheet = StyleSheet.create({
  container: {
    height: 40,
    width: 40,
    display: 'flex',
    backgroundColor: "white",
    justifyContent: "center",
    alignItems: "center",
  },
  logo: {
    width: 24,
    height: 24,
  }
});
