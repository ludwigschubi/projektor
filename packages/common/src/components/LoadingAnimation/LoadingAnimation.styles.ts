import { StyleSheet } from 'react-native';

export const LoadingAnimationStyleSheet = StyleSheet.create({
  container: {
    height: '100%',
    width: '100%',
    zIndex: 1,
  },
  animation: {
    position: 'absolute',
  },
});
