import { HomePage } from '@projektor/common';
import React from 'react';
import { Text } from 'react-native';

const App: () => React.ReactNode = () => {
  return (
    <HomePage>
      <Text>World</Text>
    </HomePage>
  );
};

export default App;
